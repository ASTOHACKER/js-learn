//.toString()// to string 
//.join("*")//นำค่าแต่ละค่าในตัวแปร Array มารวมกกันเป็นข้อความและส่งค่ากลับเป็นข้อความที่มีตัวคั่นตัวแปร แต่ละค่าตามที่กำหนด
//color.pop();//เอาตัวแปรท้าย
//let x = color.pop // เอาตัวเเปรแรกออกแล้วเก็บไปในตัวแปร x
// typeof

let color = ["Dang","Blue","Yellow"];
// let x = color.length;
//let x = color.join("*")
//let x = color.pop //
color.pop();
console.log(color)
console.log(typeof(x))
console.log(color.toString())
console.log(x)
6:22

